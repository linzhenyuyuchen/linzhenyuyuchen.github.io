---
layout:     post
title:      安装常用python库
subtitle:   pip
date:       2019-10-06
author:     LZY
header-img: img/whatisnext.jpg
catalog: true
tags:
    - Python
---


```
pip install pydicom
pip install pandas
pip install numpy==1.16.0
pip install opencv-python
pip install matplotlib
pip install tqdm
pip install pillow

pip install keras

pip install efficientnet

pip install tensorflow-gpu
pip install --upgrade tensorflow-gpu

pip install --upgrade tensorflow-hub
```

```
conda install -c conda-forge/label/gcc7 keras-applications
```

```
pip install pydicom && pip install pandas && pip install numpy==1.16.0 && pip install opencv-python && pip install matplotlib && pip install tqdm && pip install pillow && pip install albumentations
```